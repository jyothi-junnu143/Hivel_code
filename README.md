# Hivel_code
This Java project calculates a final constant C based on data extracted from a JSON structure. It handles large number conversion using BigInteger and maintains precision during division using BigDecimal.

The final output prints two calculated constant values (C1 and C2), each rounded to two decimal places
